# user_class.py defines the class which stores user information for the site when they create a profile. 
# The data of each instance is stored in the user_profiles.db database.

class User:
    
    def __init__(
        
    )